package vorbispak

import (
	"fmt"

	"github.com/yassi-github/vorbispak/cut"
)

func readByteFileAll(path string) (body []byte, err error) {
	// 
}

func writeByteFileAll(path string, body []byte) (err error) {
	// 
}
